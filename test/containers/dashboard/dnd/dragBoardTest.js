import { renderComponent, expect } from '../../../test_helper';
import Dragboard from 'components/dashboard/dnd/dragBoard';

/*describe('#dnd: Drag Board (Container)', () => {
    let container;

    beforeEach(() => {
        container = renderComponent(Dragboard);
    });

    it('has the correct class .drag-element', () => {
        expect(container).to.have.class('drag-element');
    });
});*/

//I dont know how to test